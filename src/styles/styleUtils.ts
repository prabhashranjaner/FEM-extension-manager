import { css } from "styled-components";

export const flexCenter = css`
  display: flex;
  align-items: center;
  justify-content: center;
`;

export const focusMode = css`
  outline: 1px solid var(--col-danger-hover);
  outline-offset: 3px;
`;
